package tarsila.costalonga.notasapp.ui.addfragment

/*
class AddViewModelTest {

    @get:Rule
    var instantTaskExecutorRule = InstantTaskExecutorRule()

    private lateinit var viewModel: AddViewModel

    @Before
    fun setup() {
        viewModel = AddViewModel(FakeNotasRepositorio())
    }

    @ExperimentalCoroutinesApi
    @Test
    fun `inserir nota`() = runBlockingTest {

        val nota = Notas(titulo = "casa", anotacao = "anotacao", ordem = 1)
        viewModel.insertNota(nota)

        val j = viewModel.repositorio.getTodasNotas()

        assertThat(j.isNotEmpty())
    }


}*/
